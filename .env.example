# 本地开发用（从源码目录挂载时生效）。
# 市场 / npm 安装请改用环境变量：export DASHSCOPE_API_KEY=...（详见 README「配置」）。
DASHSCOPE_API_KEY=sk-your-bailian-key       # 阿里云百炼，Qwen-VL 看图（强烈建议）
DEEPSEEK_API_KEY=sk-your-deepseek-key       # DeepSeek，OCR 文本结构化回退（可选）

# 可选：自定义电价/造价假设文件路径（升级安全，覆盖包内 data/tariff.json）
# DSH_ESS_TARIFF_PATH=/path/to/my-tariff.json
